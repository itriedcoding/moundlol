# Convex deployment URL - replace with your actual deployment URL
# Run 'npx convex dev' to get the development URL
# For production, use your production Convex deployment URL
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex site URL for auth (use your production domain in production)
CONVEX_SITE_URL=https://mound.lol

# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=dev:your-deployment

# VLY App ID and Monitoring (if using VLY integrations)
VITE_VLY_APP_ID=your-app-id
VITE_VLY_MONITORING_URL=https://runtime-monitoring.vly.ai/runtime-error
